/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['static.wixstatic.com','images.prismic.io'],
    },
};

module.exports = nextConfig;
